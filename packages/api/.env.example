# Pinata (private IPFS) — required
PINATA_JWT=
PINATA_GATEWAY=

# Auth — set a long random string (do NOT reuse the dev default)
AUTH_SECRET=

# Allowed browser origin (your deployed web app)
WEB_ORIGIN=https://web-seven-theta-63.vercel.app

# Port the server listens on
PORT=8787
