# Dedicated Base Sepolia RPC (Alchemy/Infura) — public RPC will rate-limit historical sync
PONDER_RPC_URL_84532=

# Production database (Postgres). Omit to use local pglite (dev only, not for a server).
DATABASE_URL=

# Port for GraphQL + /health
PORT=42069
